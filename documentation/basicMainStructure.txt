Broad Goals :

	Old Main Structure :
		int main()
		{
		Windowinnit();
		gladInnit();
		ImguiInnit();

		LoadSceneData();

		ProcessHdrMap(); // some draw calls 

		CreateInstanceBuffer();
		CreateShadowMapTextures();
		CreatePostProcessFrameBuffers();
		SetStaticShaderParameters();

		while(window.open)
		{
			processKeys();
			ImguiConfig();
			updateSceneData();
			updateInstanceBuffer();
			updateCameraFrustum();
			cull();

			drawShadows();
			drawOpaqueObjectsInstanced();
			drawTransparentObjects();
			drawPostProcess();
			drawImgui();
		}
		
		}

	new structure :
		int main()
		{
			Settings settings = LoadSettings();
			Renderer renderer = Renderer.getInstance();
			renderer.settings = &settings;
			vector<RObject*> objects = LoadScene(); // loads all textures,models,meshes,shaders
			renderer.addToRenderList(objects);
			while(window.open)
			{
				
			}
		}


What was I doing again ?

		
