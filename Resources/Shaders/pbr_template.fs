Step 0. Different templates // alphabetically aranged

bool shadows;
bool texture_diffuse1;
bool texture_normal1;
bool texture_roughmetal1;
bool texture_specular1;

Step 1. simple as adding a #define or const to the top of a template

int numOfPointLights;
int numOfDirectionalLights;
int numOfSpotLights;

